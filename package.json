{
  "name": "sn-rights-signals-worker",
  "version": "1.6.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "deploy": "wrangler deploy --var SN_VERSION:$npm_package_version"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.9.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0"
  },
  "overrides": {
    "undici": "^7.29.0",
    "postcss": "^8.5.25",
    "sharp": "^0.35.0",
    "ws": "^8.21.2"
  }
}
